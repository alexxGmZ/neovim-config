return {
   { "saghen/blink.compat" },
   {
      "saghen/blink.cmp",
      version = 'v0.5.1',
      dependencies = {
         "rafamadriz/friendly-snippets",
         "f3fora/cmp-spell"
      },
      opts = {
         keymap = {
            preset = "default",
            ["<Tab>"] = { "select_next", "fallback" },
            ["<S-Tab>"] = { "select_prev", "fallback" },
            ["<CR>"] = { "accept", "fallback" }
         },
         nerd_font_variant = "normal",
         sources = {
            completion = {
               enabled_providers = { "lsp", "path", "snippets", "buffer", "spell" },
            },
            providers = {
               spell = {
                  name = "spell",
                  module = "blink.compat.source",
                  score_offset = -3,
                  opts = {
                     keep_all_entries = false,
                     enable_in_context = function()
                        return true
                     end
                  }
               }
            }
         },
         windows = {
            autocomplete = {
               border = "rounded",
               selection = "manual"
            },
            documentation = {
               border = "rounded",
               auto_show = true
            }
         }
      }
   }
}
